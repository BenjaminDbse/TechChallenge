{{ form_start(form) }}
<div class="d-flex justify-content-center">
    <div class="col-auto">
        <label class="visually-hidden-focusable" for="autoSizingInputGroup">{{ form_label(form.name) }}</label>
        <div class="input-group">
            {{ form_widget(form.name) }}
            <button class="input-group-text btn btn-success card-crew">
                <img src="{{ asset('build/images/plus.png') }}" alt="save">
            </button>
        </div>
    </div>
</div>
{{ form_end(form) }}